// · i % 2(z-1) == 0,
//      S(i, z) = 2
// · i % 2(z-1) > 0 && i % 2(z-1) < z,
//      S(i, z) = i % 2(z-1)
// · i % 2(z-1) > z,
//      S(i, z) = 2z - i % 2(z-1)

// 构成序列：
// z = 3 时，Si = {1, 2, 3, 2, 1, 2, 3,...}
